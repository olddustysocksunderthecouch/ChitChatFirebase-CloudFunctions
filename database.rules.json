{
  "rules": {
    ".read": "auth != null",
    "messages": {
       "$uid": {
         ".write": "$uid === auth.uid && !data.exists()"
       }
     },
    "existing_chats": {
       "$uid": {
         ".read": "$uid === auth.uid"
       }
     }
  }
}
